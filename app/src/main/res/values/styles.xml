<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
        <item name="buttonStyle">@style/Widget.AppCompat.Button.Borderless</item>
    </style>

    <style name="quizstandard" parent="AppTheme">
        <item name="android:textSize">@dimen/fontSizes</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="quiztitle" parent="quizstandard">
        <item name="android:textSize">@dimen/titleFontSize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="quizTextInsideCardView" parent="quiztitle">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="quizbuttons" parent="quizstandard">
        <item name="android:layout_width">@dimen/menu_button_width</item>
        <item name="android:layout_height">@dimen/menu_button_height</item>
        <item name="android:layout_marginTop">@dimen/margin</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>


</resources>
